using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GhostDistance : MonoBehaviour
{

 //   public GameObject Pacman;
//    public GameObject Enemy;
    public float xDistance;
    public float zDistance;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       /* float x1, x2;
        float z1, z2;
        x1 = (Enemy.transform.position.x - Pacman.transform.position.x);
        x2 = (Enemy.transform.position.x + Pacman.transform.position.x);
        z1 = (Enemy.transform.position.z - Pacman.transform.position.z);
        z2 = (Enemy.transform.position.z + Pacman.transform.position.z);

        if (Mathf.Abs(x1) > Mathf.Abs(x2))
        {
            xDistance = x1;
        }
        else
        {
            xDistance = x2;
        }
        if(Mathf.Abs(z1) > Mathf.Abs(z2))
        {
            zDistance = z1;
        }
        else
        {
            zDistance = z2;
        }*/
    }
}
